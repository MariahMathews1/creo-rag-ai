# Fictional KLS translation pairs

Every CL and G-code file in this directory is synthetic R&D fixture data. It is not derived from a real post, part, or production program.

**FICTIONAL SAMPLE — NOT FOR MACHINE USE**

The manifest defines five paired examples: one candidate, three verified historical-demo examples, and one deprecated example. Run `python -m app.scripts.seed_translation_demo` from `backend/` to import them.
